import React from 'react';
// import PropTypes from 'prop-types';

const FacebookIdentityProvider = () => {
  return <div>Continue to facebook to authenticate.</div>;
};

FacebookIdentityProvider.propTypes = {};

export default FacebookIdentityProvider;
